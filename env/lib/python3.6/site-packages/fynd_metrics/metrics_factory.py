from __future__ import absolute_import
from prometheus_client import Counter,Gauge,generate_latest,CONTENT_TYPE_LATEST

b = Counter("a_b","ab");


class Metricsfactory(object):

    def generate_metric(self,metrictype,metricname,metricdesc):

        #type 1
        print("check for metrics========",dir(globals().get("Counter"))[2].__dict__ )
        return globals()[metrictype](metricname,metricdesc)

        #type 2
        abb = Counter(metricname,metricdesc)
        return abb

    def get_pc(self):

        return generate_latest

